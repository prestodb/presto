name: Maven OWASP Dependency Check
on: [pull_request]

jobs:
  dependency-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          path: pr

      - name: Checkout base branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          path: base

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 17
          cache: 'maven'

      - name: Get date for cache key
        id: get-date
        run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: Restore OWASP database cache
        uses: actions/cache/restore@v4
        id: cache-owasp-restore
        with:
          path: ~/.owasp/dependency-check-data
          key: owasp-cache-${{ runner.os }}-${{ steps.get-date.outputs.date }}
          restore-keys: |
            owasp-cache-${{ runner.os }}-

      - name: Run OWASP check on base branch
        working-directory: base
        run: |
          mvn org.owasp:dependency-check-maven:12.1.3:aggregate \
            -DskipTests \
            -Dformat=JSON \
            -DprettyPrint=true \
            -DfailOnError=false \
            -DossindexAnalyzerEnabled=true \
            -DnvdApiAnalyzerEnabled=false \
            -DnodeAnalyzerEnabled=false \
            -DassemblyAnalyzerEnabled=false \
            -DcentralAnalyzerEnabled=false \
            -DnuspecAnalyzerEnabled=false \
            -DnvdValidForHours=168 \
            -DdataDirectory=$HOME/.owasp/dependency-check-data

      - name: Save OWASP cache after base scan
        if: steps.cache-owasp-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: ~/.owasp/dependency-check-data
          key: owasp-cache-${{ runner.os }}-${{ steps.get-date.outputs.date }}-partial

      - name: Run OWASP check on PR branch
        working-directory: pr
        run: |
          mvn org.owasp:dependency-check-maven:12.1.3:aggregate \
            -DskipTests \
            -Dformat=JSON \
            -DprettyPrint=true \
            -DfailOnError=false \
            -DossindexAnalyzerEnabled=true \
            -DnvdApiAnalyzerEnabled=false \
            -DnodeAnalyzerEnabled=false \
            -DassemblyAnalyzerEnabled=false \
            -DcentralAnalyzerEnabled=false \
            -DnuspecAnalyzerEnabled=false \
            -DnvdValidForHours=168 \
            -DdataDirectory=$HOME/.owasp/dependency-check-data

      - name: Compare and fail on new high/critical CVEs
        run: |
          # Extract only high and critical CVEs from both branches (CVSS >= 7.0)
          base_cves=$(cat base/target/dependency-check-report.json 2>/dev/null | jq -r '.dependencies[].vulnerabilities[]? | select((.cvssv2.score // 0) >= 7.0 or (.cvssv3.baseScore // 0) >= 7.0) | .name' | grep -E '^CVE-[0-9]{4}-[0-9]+$' | sort -u)
          pr_cves=$(cat pr/target/dependency-check-report.json 2>/dev/null | jq -r '.dependencies[].vulnerabilities[]? | select((.cvssv2.score // 0) >= 7.0 or (.cvssv3.baseScore // 0) >= 7.0) | .name' | grep -E '^CVE-[0-9]{4}-[0-9]+$' | sort -u)

          # Find new CVEs introduced in PR
          new_cves=$(comm -13 <(echo "$base_cves") <(echo "$pr_cves"))

          if [ -n "$new_cves" ]; then
            echo "❌ New vulnerabilities introduced in PR:"
            echo "$new_cves"
            echo ""

            for cve in $new_cves; do
              echo "=================================================="
              echo "CVE: $cve"
              echo "=================================================="

              # Find which dependencies have this CVE
              cat pr/target/dependency-check-report.json | jq -r ".dependencies[] | select(.vulnerabilities[]?.name == \"$cve\") | \"Module: \" + (.projectReferences // [\"root\"])[0] + \"\nDependency: \" + .fileName + \"\nPackage: \" + .packages[0].id + \"\nDescription: \" + (.vulnerabilities[] | select(.name == \"$cve\") | .description)"

              echo ""
            done

            exit 1
          else
            echo "✅ No new vulnerabilities introduced"
          fi

      - name: Save OWASP database cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: ~/.owasp/dependency-check-data
          key: owasp-cache-${{ runner.os }}-${{ steps.get-date.outputs.date }}

      - name: Upload reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: owasp-reports
          path: |
            base/target/dependency-check-report.json
            pr/target/dependency-check-report.json
