version: '2'
services:

  presto-master:
    volumes:
      - ../../../conf/presto/etc/environment-specific-catalogs/singlenode-kerberos-hdfs-impersonation-cross-realm/hive.properties:/docker/volumes/conf/presto/etc/catalog/hive.properties
      - ../../../conf/presto/etc/environment-specific-catalogs/singlenode-kerberos-hdfs-impersonation-cross-realm/iceberg.properties:/docker/volumes/conf/presto/etc/catalog/iceberg.properties
